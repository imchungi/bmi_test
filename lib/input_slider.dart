import 'package:flutter/material.dart';
import 'screens/input_page.dart';
/*
// 화면 안의 키 부분등 . 전체가 갱신 되지 않아서 . 다시 작성하거나 폐기 할 것
class MainSliderMenu extends StatefulWidget {
  const MainSliderMenu({super.key});

  @override
  State<MainSliderMenu> createState() => _MainSliderMenuState();
}

class _MainSliderMenuState extends State<MainSliderMenu> {
  @override
  Widget build(BuildContext context) {
    return SliderTheme(
      data: SliderTheme.of(context).copyWith(
        inactiveTrackColor: Color(0xFF8D8E98),
        activeTrackColor: Colors.white,
        thumbColor: Color(0xFFEB1555),
        overlayColor: Color(0x29EB1555),
        thumbShape:
        RoundSliderThumbShape(enabledThumbRadius: 20.0),
        overlayShape:
        RoundSliderOverlayShape(overlayRadius: 10.0),
      ),
      child: Slider(
        value: height.toDouble(),
        min: 100.0,
        max: 200.0,
        //activeColor: Colors.deepOrange,

        onChanged: (double newValue) {
          setState(() {
            height = newValue.round();
          });
        },
      ),
    );
  }
}

*/